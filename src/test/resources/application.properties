## Server Properties
server.host = localhost
## not being used by system, should be: @Value("${test.server.port}") is not getting this value, rather it becomes 0 unless set in test-class
server.port = 8081